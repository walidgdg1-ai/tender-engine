# Live Tender Snapshot

Source discovery run: `33999665947`  
Rows: **125,317**  
GPT packets: **251**  
Coverage: **PARTIAL_WORLD_COVERAGE**

Carried forward for recall: **29,308**. These rows are explicitly marked and do not repair coverage.

This branch is force-replaced on every snapshot so the repository does not accumulate hourly snapshot history. Read `discovery_coverage.json` and `GPT_WIDE_READ_INSTRUCTIONS.md`, then scan `gpt-packets/packet-*.jsonl`.
